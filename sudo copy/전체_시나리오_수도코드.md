// ============================================================
// [Python Backend - sources.py]
// ============================================================
"""
# 세션별 대화 기록을 저장할 메모리 (실제로는 Redis나 DB 권장)
session_memory = {
    "session_id_123": [
        {"role": "assistant", "content": "안녕하세요, 은행 업무 보러 왔는데요..."},
        {"role": "user", "content": "어떤 업무 도와드릴까요?"}
    ]
}

def get_simulation_response(session_id, user_message):
    # 1. 기존 히스토리 불러오기
    history = session_memory.get(session_id, [])
    
    # 2. AI에게 히스토리와 함께 '감정 상태' 분석 요청
    prompt = f"이전 대화: {history}\n사용자: {user_message}\n너는 가스라이팅 당한 고객이야. 반응해줘."
    # AI 응답 형식 예시: {"dialogue": "...", "emotion": "Angry", "tension": 85}
    
    # 3. 히스토리 업데이트 및 결과 반환
    history.append({"role": "user", "content": user_message})
    history.append({"role": "assistant", "content": ai_response['dialogue']})
    
    return ai_response
"""

// ============================================================
// [Frontend - useSimulator.js]
// ============================================================
/*
const evaluateAnswer = async (playerAnswer) => {
    // 1. 서버에 평가/대화 요청
    const result = await api.post("/simulator/evaluate", {
        session_id: userId,
        message: playerAnswer
    });
    
    // 2. 유니티로 AI 대사와 감정 상태 전달
    sendMessage("ReactReceiver", "ShowDialogue", result.dialogue);
    sendMessage("ReactReceiver", "SetEmotion", result.emotional_state);
    sendMessage("ReactReceiver", "SetTension", result.tension_level);
    
    // 3. 만약 긴장도가 100이면 '검거' 혹은 '실패' 처리
    if (result.is_terminated) {
        showResultReport(result.final_score);
    }
}
*/
